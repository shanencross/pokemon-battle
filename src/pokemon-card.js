export default class PokemonCard {
	constructor(name, hp, types, attacks, weaknesses) {
		this.name = name;
		this.hp = hp;
		this.types = types;
		this.attacks = attacks;
		this.weaknesses = weaknesses;
	}
}